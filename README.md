.tmux
=====

```
$ cd
$ git clone https://github.com/lesonky/tmuxconf.git ~/.tmux
$ ln -s -f .tmux/.tmux.conf
$ cp .tmux/.tmux.conf.local .

# need fzf
brew install fzf

# need install tmp plugin first
git clone https://github.com/tmux-plugins/tpm ~/.tmux/plugins/tpm

# open tmux and perfix I install all plugins

```


